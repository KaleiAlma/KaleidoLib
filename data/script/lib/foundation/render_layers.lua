
---@enum foundation.layers
local layers = {
    -- these probably shouldn't be objects...
    -- UI_BG = -1000,
    -- BG = -800,
    -- SPELL_BG = -700,
    ENEMY = -600,
    PLAYER_BULLET = -500,
    PLAYER = -400,
    ITEM = -300,
    ENEMY_BULLET = -200,
    ENEMY_BULLET_EF = -100,
    TOP = 0,
    UI_MAIN = 200,
    UI_TOP = 400,

    -- UI_BG_TRANSITION = -999,
    -- UI_MAIN_TRANSITION = 199,
}

return layers